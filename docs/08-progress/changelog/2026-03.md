# Progress Changelog - 2026-03

## 2026-03-30

- Menambahkan framework progress tracking berbasis Markdown:
  - Dashboard global: `08-progress/index.md`
  - Progress per modul: `rbac`, `i18n`, `subscription`
- Menambahkan section `Progress` pada sidebar docs site.
- Menetapkan cadence update progress per PR/merge.

## 2026-03-31

- Menyelesaikan migrasi dari Path-based routing `/t/{tenant}` ke Subdomain-based routing `https://{tenant}.toko-baru.com/`.
- Memperbaiki Error 404 pada modul-modul (Invitations, Members, WhatsApp) dengan dinamisasi rute API menggunakan helper `tenantRoute.apiTo()`.
- Memperbaiki error CORS saat `Redirect` login lintas subdomain menggunakan fitur `Inertia::location()`.
- Mengkonfigurasi Laravel Reverb agar menggunakan port 443 via HTTPS dan host dinamis sesuai origin browser (`window.location.hostname`), bukan hardcoded IP.
- Global Replace string path lama `/t/{tenant}/` menjadi skema URL modern di seluruh Markdown Docs.
